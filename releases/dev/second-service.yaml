---
apiVersion: flux.weave.works/v1beta1
kind: HelmRelease
metadata:
  name: second-service
  namespace: demo
  annotations:
    flux.weave.works/automated: "true"
    flux.weave.works/tag.chart-image: glob:0.0.*
spec:
  releaseName: second-service
  chart:
    git: ssh://git@github.com/alexmelkor/flux-get-started.git
    ref: master
    path: charts/basic-chart
  values:
    fullnameOverride: second-service
    image:
      repository: registry.battery-park.conductor.com/second-demo-service
      tag: 0.0.3
    resources:
      requests:
        memory: 128Mi
        cpu: 10m
    serviceType: ClusterIP
